package config

import (
	"encoding/json"
	"errors"
	"os"
	"plugin"

	"github.com/webability-go/xconfig"

	"github.com/webability-go/xamboo/assets"
	"github.com/webability-go/xamboo/utils"
)

var OverConfig = &ConfigDef{}

func overload(file) error {
	err := OverConfig.Load(file)
	if err != nil {
		return err
	}

	//

}
